# Infrastructure

Documentation of the shared AWS lab used by every project.

Planned contents: VPC and subnet layout, route tables, security group rules, server inventory (Bastion, NAT instance, App Server 1, App Server 2, Monitoring Server), and access method (SSH through the Bastion).
